#include<stdio.h>
#include<string.h>


void add(char ch, str, i+1,j){


		
	return ;}

void perm(char[] str, int i int j){
	add(str[i], str,i+1,j);	
	return ;}

void generate_perm(char[] str){
	int len = strlen(str);
	perm(str,0,len-1);
	return ;}

int main(void){

	generate_perm("abc");
	return ;}
